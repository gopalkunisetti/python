import random
from art import logo
# cards = [11,2,3,4,5,6,7,8,9,10,10,10,10]
# random1 = random.choice(cards)
# random2 = random.choice(cards)
# score = random1 + random2
# #decision = input("Do you want a play a game of BLACKJACK?Type YES or NO ").lower()
# print(f"Your cards:({random1},{random2}),current score:{score}")
# computer_random1 = random.choice(cards)
# computer_random2 = random.choice(cards)
# computer_score = computer_random1 + computer_random2
# print(f"computer's choice:{computer_random1}")
# if score == 21:
#     print("User win")
# elif computer_score == 21:
#     print("computer win")
# if score > 21:
#     if 11 in cards:
#         temp_nums = [1 if x == 11 else x for x in cards]
#         print(cards)
#         print(temp_nums)
#     else:
#         print("computer win")
#
# yes_or_no = input("Type Y to get another card or Type N to pass").lower()
def deal_card():
    cards = [11, 2, 3, 4, 5, 6, 7, 8, 9, 10, 10, 10, 10]
    card = random.choice(cards)
    return card

def calculate_score(cards):
    if sum(cards) == 21 and len(cards) == 2:
        return 0
    if 11 in cards and sum(cards) > 21:
        cards.remove(11)
        cards.append(1)
    return sum(cards)

def compare(u_score,c_score):
    if u_score == c_score:
        print("draw")
    elif c_score == 0:
        print("computer has BLACKJACK,computer win")
    elif u_score > 21:
        print("you went over,computer win")
    elif u_score == 0:
        print("you have BLACKJACK,you win")
    elif c_score >21:
        print("computer went over,you win")
    elif u_score > c_score:
        print("you win")
    else:
        print("computer win")

def play_game():
    print(logo)
    user_cards = []
    computer_cards = []
    user_score = -1
    computer_score = -1
    is_game_over = False

    for _ in range(2):
        user_cards.append(deal_card())
        computer_cards.append(deal_card())

    while not is_game_over:
        user_score = calculate_score(user_cards)
        computer_score = calculate_score(computer_cards)
        print(f"your cards are: {user_cards} ,and score is: {user_score}")
        print(f"computer's first card is: {computer_cards[0]}")
        if user_score == 0 or computer_score == 0 or user_score > 21:
            is_game_over = True
        else:
            yes_or_no = input("type YES to get another card or NO to see the result").lower()
            if yes_or_no == "yes":
                user_cards.append(deal_card())
            else:
                is_game_over = True
    while computer_score != 0 and computer_score < 17 :
        computer_cards.append(deal_card())
        computer_score = calculate_score(computer_cards)
    print(f"your final hand: {user_cards}, final score: {user_score}")
    print(f"computer final hand: {computer_cards}, computer score: {computer_score}")
    compare(u_score=user_score,c_score=computer_score)
while input("Do you want to play a game of blackjack game?tpe yes or no").lower() == "yes":
    print("\n" *20)
    play_game()
